import 'package:flutter/material.dart';
import 'pages/packing_formula_list_page.dart';

class PackingFormulaScreen extends StatelessWidget {
  const PackingFormulaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PackingFormulaListPage();
  }
}
